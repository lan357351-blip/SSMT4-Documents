# 仅保留数字顶点组

## 功能说明

删除所有名称不是纯数字的顶点组，只保留数字命名的顶点组。

## 执行的操作

1. 遍历所有选中的网格物体
2. 检查每个顶点组的名称是否为纯数字
3. 删除名称不是纯数字的顶点组

## 效果

- 只保留名称为纯数字（如 `0`、`1`、`23`）的顶点组
- 所有非数字名称的顶点组被删除

## 适用场景

- 游戏引擎只接受数字索引的骨骼/顶点组时
