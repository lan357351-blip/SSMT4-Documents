# 用模型名称作为前缀重命名顶点组

## 功能说明

将选中物体的所有顶点组名称添加"模型名_"前缀。例如物体名为 `Body`，顶点组 `0` 将被重命名为 `Body_0`。

## 执行的操作

1. 遍历所有选中的网格物体
2. 对每个物体的每个顶点组，在名称前添加 `{物体名}_` 前缀
3. 使用 Blender 的顶点组重命名接口执行修改

## 效果

- 所有顶点组名称变为 `物体名_原名称` 格式
- 可用于区分不同物体的同名顶点组

## 适用场景

- 合并多个物体前，需要区分各自的顶点组
- 需要追踪顶点组来源物体
