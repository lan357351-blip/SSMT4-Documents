# 批量重命名顶点组

## 功能说明

在所有选中的网格物体中，将指定旧名称的顶点组重命名为新名称。

## 参数

| 参数 | 说明 |
|------|------|
| 旧名称 | 需要被替换的顶点组名称 |
| 新名称 | 替换后的顶点组名称 |

## 执行的操作

1. 遍历所有选中的网格物体
2. 在每个物体中查找名称匹配"旧名称"的顶点组
3. 如果新名称的顶点组已存在，跳过该物体避免冲突
4. 否则将顶点组重命名为新名称

## 效果

- 所有选中物体中匹配的顶点组被统一重命名
- 如果目标名称已存在则跳过，不会覆盖
